# Ograniczony przegląd semantyczny — phase3-supply-chain-gate-fix (AniNeko + łańcuch dostaw)

- DATA: 2026-09-14-122846
- ZAKRES: `AniNekoExtractor.kt`, `AniNekoTransport.kt`, `AnimeScraperService.kt`, `AnimeScrapeContracts.kt`, `AniNekoExtractorContractTest.kt`, `AniNekoTransportTest.kt`, `AnimeScraperServiceAggregationTest.kt`, `AnimeScraperTest.kt`, `app/build.gradle.kts` (runner), `AniNekoLiveIntegrationTest.kt`, dane testowe `app/src/test/resources/anineko/*`, `TrackingSettingsOverviewTest.kt` (dryf blokujący kompilację androidTest).
- KONTEKST (tylko odczyt): `build.gradle.kts`, `settings.gradle.kts`, `verifyDependencySupplyChain` (4/222/568 + 15/15 + 28/28), locki/metadany weryfikacji, `gradle/DETACHED_TOOLING_AUDIT.md`, świeże XML bramek, `.kiro/FOX_IMPLEMENTATION_STATE.md`.

## Ocena zgodnie z wymaganiami zadania 8

1. **Izolacja sieci publicznej** — każdy test klienta ma interceptor sieciowy-zwiadowę rzucający `AssertionError` dla hosta innego niż `MockWebServer`; licznik `publicNetworkTransportRequests==0` asertowany w każdym przepływie; FTP/TEST-NET case potwierdza brak transportu publicznego. **PASS.**
2. **Fałszywie pozytywne media** — polityka: ścieżka `.m3u8` lub (MIME HLS + pierwsza linia `#EXTM3U`); fragment/query bez ścieżki → sonda; sonda zwracająca HTML bez `#EXTM3U` → `UnsupportedMedia`; sonda 500 → `HttpStatus`; brak rozszerzenia + poprawne MIME+`#EXTM3U` → akceptacja. **PASS.**
3. **Semantyka URL-i względnych/końcowych i nagłówków** — iframe/`data-video` rozwiązane względem finalnego URL-a watch (po 302), HLS/tracki względem finalnego embedu; UA/Referer/Origin asertowane per żądanie i per strumień; brak CR/LF. **PASS.**
4. **Anulowanie i własność wywołania** — `invokeOnCancellation { call.cancel() }`, atomowy właściciel callbacka, podwójne wznowienie niemożliwe; w serwisie `channelFlow`+`coroutineScope`+`joinAll`, anulowanie kolektora anuluje dzieci i zakazuje późnego `send` (trzy przypadki: przed wywołaniem / w trakcie / przed emisją — zielone). **PASS.**
5. **Naturalne zakończenie dostawców i mechanizm awaryjny** — `joinAll` bez zegara; błąd jednego dostawcy nie zatrzymuje równoległych (isolation test); `runTest` dowodzi zakończenia sygnałami dostawców (wirtualny `delay(60_000)` przepływa, realny zegar nieistotny). **PASS.**
6. **Uczciwe statusy live** — dokładnie jeden znacznik `FOXTV_LIVE_ANIME_STATUS`; brak zgody → `NOT_RUN` + assumption; BLOCKED/FAIL zawodzą bramkę; PASS tylko po klasyfikacji HLS z sondą manifestu nagłówkami ekstraktora; brak zakodowanego URL-a strumienia; brak `@Ignore`. **PASS.**
7. **Brak regresji sześciu napraw łańcucha dostaw** — `verifyDependencySupplyChain` exit 0: `4/222/568 + 15/15 + 28/28`; dokumentacja `lock 0/15` + `PROVENANCE_GAP` niezmieniona; wrapper-validation w workflow aktywna; delta `git status` względem bazy zadania 2 = wyłącznie pliki tej poprawki (2 nowe pliki + RM TrackingSettingsOverviewTest); `libs.versions.toml`/wrapper/locki nietknięte przez tę poprawkę. **PASS.**

## Ustalenia i rozstrzygnięcia

| ID | Waga | Ustalenie | Rozstrzygnięcie |
|---|---|---|---|
| F-A1 | P1→**RESOLVED** | Wewnętrzny konstruktor testowy `AnimeScraperService` tworzył anonimową podklasę finalnej `HttpStreamLivenessValidator` (nie kompiluje się) i nadpisywał `val` przez refleksję. | Konstruktor przepisany na `internal var` + bezpośrednie przypisania + wstrzykiwalny dispatcher; brak refleksji (grep potwierdza). Dowód: zielony compile + 23/23 targeted gate. |
| F-A2 | P2→**RESOLVED** | Sonda manifestu używała `HEAD` (puste body) — ścieżka akceptacji MIME+`#EXTM3U` była martwa. | Sonda zmieniona na `GET`; pozytywny test bez rozszerzenia (Test D) i negatywny MIME-bez-`#EXTM3U` (Test E) zielone. |
| F-A3 | P2→**RESOLVED** | Ścieżka iframe gubiła ścieżki napisów (`byAudio` trzymał tylko URL) — strumień z embedu wychodził z `tracks=[]`. | Dodany `tracksByUrl` (URL→tracki) w `extractWithOutcome`; Test A/B asertują dokładnie 4 tracki; property2 zielone. |
| F-A4 | P2→**RESOLVED** | `TrackingSettingsOverviewTest` (androidTest): `FOXTV_SYNC` usunięte z enumu, `onAnimeIdClick` wymagane, `onSync` usunięte z `TraktAccountDialog` — blokada kompilacji androidTest, wcześniejszy dryf względem ewolucji sygnatur produkcyjnych. | Test wyrównany do bieżących sygnatur (`SIMKL`, `onAnimeIdClick={}`, bez `onSync`); `:app:compileDebugAndroidTestKotlin` exit 0. |
| F-B1 | P3 | Konstruktor testowy serwisu deleguje do primary i nadmiarowo buduje produkcyjną fabrykę przed nadpisaniem. | `nie dotyczy` jako defekt: brak I/O w konstruktorach ekstraktorów (grep `init {` = tylko limit transportu); koszt pomijalny, zachowanie bez zmian. |
| F-B2 | P3 | Dedup tracków porównuje resolved URL z raw `src` (duplikat raw-src nie jest dedupowany). | `nie dotyczy` jako zachowanie zachowane (observation-first); zakodowane w fixturach i asercjach świadomie. |
| F-B3 | P3 | `internal var` pól kontraktowych serwisu. | `nie dotyczy`: mutacja tylko przez wewnętrzny konstruktor testowy; brak innych punktów zapisu w module (grep). |
| F-B4 | P3 | `AniNekoExtractor.extract` loguje przez `Log.w`. | `nie dotyczy`: JVM suite używa wyłącznie `extractWithOutcome`; ścieżka produkcyjna bez zmian względem obserwacji. |

## Werdykt

- **OPEN P1 = 0**
- **OPEN P2 = 0**
- P3: 4 pozycje z udokumentowanym `nie dotyczy` (kod/test/dowód wskazane powyżej).
- Bramki: targeted 23/23 (świeże, `--rerun-tasks --no-build-cache --no-configuration-cache --dependency-verification=strict --max-workers=1 --no-parallel --no-daemon`), pełna JVM 1344/0/0/1 (dokładnie jeden skip: `FrameRateUtilsAfrTest.live test real extensionless debrid MP4 URL detects frame rate`), androidTest compile exit 0, supply chain exit 0 (`4/222/568 + 15/15 + 28/28`).
- `DEVICE RUNTIME NOT VERIFIED`; `LIVE_NETWORK = NOT_RUN` (bramka androidTest nieskompilowana na urządzeniu).
